export const enum LocalStorageEnum {
  navigationFold = 'ng-navigation-fold',
  isLoginClicked = 'is-login-clicked', // TODO[later]: Should be removed when login actions done in angular
  rfRefreshToken = 'rf-refresh-token',
  rfRefreshTokenExpired = 'rf-refresh-token-expired',
}
